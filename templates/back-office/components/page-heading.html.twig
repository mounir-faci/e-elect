{% trans_default_domain 'translations' %}

{% set title = title is defined ? title : 'back.default.pages.heading' %}

<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h4 class="h4 mb-0 text-gray-800">{{ title | trans }}</h4>
</div>
